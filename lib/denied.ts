// 권한이 없어 화면을 못 여는 사람이 **어디로 가는가** (MD-P-2026-041 §B).
//
// ── 왜 한 곳에서 정하는가 ────────────────────────────────────────
//
// 지금껏 네 화면(구성원 · 에이전트 흔적 · 설정 · 업무 현황)이 각자 `/assistant`
// 로 밀어냈다. 그 화면이 **없어졌다.** 네 군데에 흩어져 있었으므로 하나라도
// 빠뜨리면 **갈 곳 없는 리다이렉트**가 남는다 — 막힌 사람이 404 를 본다.
//
// 「링크의 조건과 화면의 조건은 같은 함수에서 온다」의 옆자리다(§G):
// **막은 사람이 가는 곳도 한 곳에서 낸다.** 그래야 목적지가 사라질 때
// 고칠 자리가 하나다.
//
// ── 왜 팀 홈인가 ─────────────────────────────────────────────────
//
// 로그인한 사람이면 누구나 볼 수 있는 화면이라야 한다. 다시 튕기면 고리가 된다.
// `/`(팀 홈)는 역할 게이트가 없다 — 그래서 여기다.
//
// ⚠ 이 이동은 **이유를 말하지 않는다.** 누른 사람은 왜 홈에 왔는지 모른다.
//   `/assistant` 로 보내던 때도 마찬가지였으므로 이번에 나빠진 것은 아니지만,
//   고쳐진 것도 아니다. 남은 거친 자리로 적어 둔다(백로그 B-31).
export const DENIED_HREF = "/";
