# MD-P-2026-031 — 시각 밀도와 위계 (개정 1 · 028 종결 포함)
028 sort_order 정규화 확정 · 폰트 스케일 · 빈 가로 공간 금지 · 홈 재편 · 색/버튼/칩/정렬 · 모션 확장
작성 2026.08.09 / 권정혁 · PM

> **첨부.** `missiondeck_home_v6.html` (홈 시안). §C·§D·§F 는 그 파일이 기준이다.
> 마크업을 베끼지 말 것. **결정을 옮기는 것이지 HTML 을 옮기는 것이 아니다.**
> 시안의 값과 이 문서의 값이 다르면 **이 문서가 이긴다.**

> **보고 형식.** 완료 보고를 여러 메시지로 나누지 말 것. 한 번에 하나로 제출한다.

---

# §0 — MD-P-2026-028 종결

## 0-1. 병합 승인
`de26aa0` 확인했다. 마이그레이션 0건 · build · tsc 통과. 종결한다.

## 0-2. C-b 묶는 단위 — **[A] 형제 전체(parent_task_id) 로 확정**

[B] 를 지운다. 이유는 셋이다.

1. **저장 단위와 읽기 단위가 같아야 한다.** `/tasks` 의 "직접 정한 순서"도
   `POST /api/tasks/reorder` 도 `parent_task_id` 로 형제를 모은다. [B] 는
   화면이 읽지 않는 단위에 번호를 매기는 것이다.
2. **[B] 는 오래가지 않는다.** 누가 한 번 드래그하면 그 형제 전체가 [A] 형태로
   다시 쓰인다. [B] 로 정리해 두면 그 순간 다시 섞인다.
3. **[A] 는 순서를 바꾸는 게 아니라 굳히는 것이다.** 화면은 이미 전역으로
   `sort_order` 를 읽고 있으므로, 프로젝트별로 겹친 1..N 이 만들어 낸 지금의
   섞인 순서가 **현재 사용자가 보고 있는 순서**다. [A] 는 그것을 고유값으로 고정한다.

## 0-3. 실행 절차 — 순서를 지킬 것

**⓪ CC 선작업.** `01_sort_order_normalize.sql` 에서 **②-B(프로젝트별)를 파일에서 삭제**한다.
주석 처리로 남기지 말 것. 나중에 누가 주석을 풀 수 있다.
②-A 만 남기고 파일명을 `01_sort_order_normalize_A.sql` 로 바꾼다.

**① PM 이 Neon 에서 읽기 전용 확인.** 다음 세 숫자를 확인한 뒤에만 다음으로 간다.
- `sort_order = 0` 인 행이 몇 건인가
- 그 행들이 정규화 후 몇 번째 자리로 가는가
- `sort_order ≠ 0` 인 행 중 **자리가 바뀌는 것이 0건**인가

**⚠ 0 인 행은 맨 앞에서 맨 뒤로 실제로 이동한다.** 이것은 부작용이 아니라 의도다.
0 은 "순서를 정한 적 없음"이지 "1등"이 아니다. 다만 사람이 그 이동 폭을
**실행 전에 눈으로 보고** 넘어가야 한다.

**② 스냅샷 → ③ UPDATE → ④ 사후 확인.**
사후 확인에서 다음을 제출한다.
- 전역 목록에서 `sort_order` 중복이 0건인가
- 정규화 전후의 상위 20행 순서 비교표 (id · 제목 · before · after)
- 0 이었던 행이 어디로 갔는지 목록

## 0-4. 실행 전 CC 가 먼저 답할 것 (한 줄씩)
- `/tasks` 목록 쿼리의 실제 `ORDER BY` 절 **원문**. `sort_order = 0` 과 `NULL` 을
  각각 어떻게 취급하는가. (지금 화면이 0 을 앞에 놓는다는 것을 코드로 증명할 것)
- `sort_order` 가 `NULL` 인 행이 있는가. 있다면 0 과 같이 처리되는가.
- 하위 업무(`parent_task_id` 가 있는 것)도 이번 정규화 대상인가. 대상이면
  부모별로 1..N 이 독립적으로 매겨지는가.

## 0-5. 백로그 처분
- **B-11 AreaView 도달 불가 → 되살린다.** 단 옛 구현을 살리는 것이 아니다.
  §C4 의 공용 컴포넌트로 **다시 만든다**(영역 필터가 고정된 기한 막대 목록).
  옛 `AreaView` 는 삭제한다. 진입 경로는 사이드바 영역 항목 + 행의 영역 표시 클릭.
- **B-12 「팀 › 업무」 assignee 빈 값 → 이번에 함께 고친다.** §D5 에서 담당자 렌더링을
  전부 손대므로 같은 자리다.
- **B-15 분기 헤더 중첩 button → 이번에 함께 고친다.** §E3 으로 옮긴다.
- B-5 · B-8 · B-10 · B-13 · B-14 는 그대로 백로그에 둔다.

---

# §1 — 문제

프로덕션을 **100% 배율**로 열었을 때 세 가지가 동시에 보인다.

**하나. 글자가 작다.** 본문 12.5px, 보조 11.5px, 최소 10.5px. 27인치에서 팀원이 읽는
화면이다. 밀도를 위해 줄인 것이 아니라 기본값을 안 정한 채 흘러왔다.

**둘. 가로가 비어 있다.** 홈 타임라인 아래 목록이 행 하나에 정보 셋을 담고 오른쪽
700px 이상을 비운다. 여백이 아니라 미완성이다. 원인은 024~030 에서
"목록 = 38px 행, 전체 폭"이라고만 정하고 **그 폭을 무엇으로 채우는지**를 안 정한 것이다.
내 지시서의 결함이다.

**셋. 규격 밖 요소가 쌓였다.** `docs/audit/031/` 에 이미 나와 있다.
세로 정렬 549건 · 버튼 416개 중 규격 밖 179개 · 칩 14종.

---

# §A 폰트 스케일과 행 높이

### A1. 스케일을 올린다
6단계 구조는 유지하고 값만 올린다. **단계를 늘리지 말 것.**

| 토큰 | 현재 | 변경 | 쓰임 |
|---|---|---|---|
| `--f1` | 25px | **26px** | 화면 제목 |
| `--f2` | 19px | **20px** | 카드/섹션 제목 |
| `--f3` | 14px | **15px** | 강조 본문 · 큰 수치 |
| `--f4` | 12.5px | **13.5px** | **본문 기본** · 목록 제목 |
| `--f5` | 11.5px | **12px** | 보조 · 수치 |
| `--f6` | 10.5px | **11px** | 라벨 · 메타 |

- `--f6` 아래는 만들지 않는다. **11px 미만 글자는 화면에 존재하지 않는다.**
- 하드코딩 `font-size` 를 전수 조사해 토큰으로 바꾼다. 개수 before/after 제출.
- **배율 100% 기준으로 검증한다.** 125% 에서 잘 보이는 것은 근거가 아니다.

### A2. 행 높이
- 목록 행 38px → **44px** / 그룹·섹션 헤더 34px → **38px**
- 이유는 밀도 완화가 아니라 **담당자 이름과 상태 알약이 들어갈 자리**다(§D).
- 44px 은 화면당 표시 건수를 줄인다. 그 대가는 §C1 이 회수한다.

### A3. 세로 정렬 — 549건
- 상자·셀·버튼 안 텍스트는 **예외 없이 세로 중앙**.
- 고치는 방법을 하나로 통일한다. `display:flex; align-items:center` 이거나,
  단일 줄은 `height` 와 같은 `line-height`. **한 컴포넌트에 두 방법을 섞지 말 것.**
- **여러 줄 텍스트 셀은 상단 정렬이 맞다.** 그 경우는 목록으로 따로 제출.
  무조건 중앙으로 밀지 말 것.

---

# §B 빈 가로 공간 금지

### B1. 규칙
> **전체 폭을 쓰는 행은, 그 폭을 정보로 채우거나 폭을 줄인다.**
> 오른쪽 25% 이상이 빈 채로 남는 전체 폭 행은 규격 위반이다.

- 채울 정보가 없으면 `max-width` 로 줄이고 왼쪽 정렬한다.
- 억지 여백(`space-between` 으로 양 끝에 두 개만 두는 것)도 위반이다.

### B2. 검사
- 정적 검사기 `density-audit` 를 만든다. `motion-audit` 옆에 둔다.
- 판정: 전체 폭 컨테이너의 마지막 자식 오른쪽 끝 ~ 컨테이너 오른쪽 끝 거리가
  컨테이너 폭의 **25% 초과**면 위반.
- 예외는 주석이 아니라 **허용 목록 파일**에 사유 한 줄과 함께 남긴다.
- **검사를 만들 수 없으면 규격이 아니라 취향이다.** 검사가 먼저다.

---

# §C 홈 재편

시안 `missiondeck_home_v6.html` 의 3층 구조를 간다.

### C1. 1층 — 판단 타일 4개

| 타일 | 뜻 | 왜 필요한가 |
|---|---|---|
| 지연 | 기한이 지났고 안 끝남 | 지금 손해가 나고 있는 것 |
| 이번 주 마감 | D-0 ~ D-7 | 이번 주에 끝내야 하는 것 |
| 막힘 | `blocked_by` 가 걸린 것 | 내가 손대도 안 움직이는 것 |
| **내가 막는 것** | 내 업무를 원인으로 남이 막힌 것 | **남의 시간을 쓰고 있는 것** |

- 네 번째가 이번 개편의 핵심이다. 028 §B2 역방향 차단이 이미 서버에 있다.
- 각 타일은 **숫자 + 한 줄 사유**를 갖는다. `3건 · 조서연이 기다리는 중`
  숫자만 있는 타일은 눌러보기 전까지 아무 판단도 못 하게 한다.
- 0건 타일은 숨기지 않고 회색으로 남긴다. 사라지면 어제와 비교가 안 된다.
- 클릭 = 그 조건이 걸린 목록으로 이동. **필터 상태가 URL 에 남아야 한다.**

### C2. 2층 — 진행 중인 일 (기한 막대 목록)
- **메인/상시를 나눈다.** R&D · 플랫폼이 메인, 연구소 · 기타 · 현장실습은 상시다.
  기본은 메인만 펼침. 상시는 접힌 섹션 하나로 건수만 보인다.
  단, 상시 중 우선순위 `높음` 은 메인 섹션 맨 아래로 끌어올린다.
- 묶는 기준 셀렉트 3택: `프로젝트 · 우선순위 · 기한`. 기본 프로젝트.
- 프로젝트 그룹 헤더는 **롤업**: 색 점 · 이름 · `영역 · N건 · 기간` · 담당자 이름 ·
  롤업 막대 · 퍼센트. 롤업 값은 `lib/progress.ts` 를 그대로 쓴다. **다시 계산하지 말 것.**
- 월 눈금자(1 · 8 · 15 · 22 · 31)를 목록 위 한 줄. 막대 위치가 읽히게 한다.

### C3. 3층 — 오른쪽 레일 320px
- 오늘의 활동 · 내 목표 진척 · 최근 본 것. 스크롤은 본문과 독립.
- 레일이 §B1 의 빈 공간을 회수하는 장치다. **레일을 빼면 §B1 위반이 다시 난다.**

### C4. 재사용 — 이 작업의 성패
2층의 기한 막대 목록은 홈 전용이 아니다.
**홈 · 업무 목록 · 프로젝트 상세 · 영역 상세(B-11 재생성분) 넷이 같은 컴포넌트를 쓴다.**
홈에만 만들고 나머지를 옛 목록으로 두면 이 작업은 실패다.
진척 계산기가 9가지로 갈렸던 것과 정확히 같은 경로다.

---

# §D 목록 행 규격

### D1. 영역 표시 — 상자를 없앤다
- 테두리 배지 → **6px 색 사각형(radius 2) + `--f6` `--muted` 텍스트**
- 한 행에 알약이 셋 이상이면 어느 것도 안 읽힌다. 알약은 예외 상태 전용이다.

### D2. 상태 알약 — 테두리 없이 틴트만
- **다섯 종만 남긴다.** `지연` `오늘` `D-N` `막힘` `내가 막는 중`
- `border` 없음. 배경은 옅은 틴트, 글자는 진한 쪽. 10px / 700 / radius 20 / padding 3px 8px.
- **칩 14종 → 5종.** 나머지 9종이 어디서 왜 쓰였는지 목록으로 제출하고,
  대체안(텍스트 · 색 점 · 삭제) 중 무엇으로 갔는지 표로 낼 것.

### D3. 좌측 색 띠 금지
- 카드·섹션 제목 왼쪽 3px 색 바를 **전부 제거**한다.
- 제목 무게는 타이포로 만든다. `--f3` 이상 / `font-weight:800` / `letter-spacing:-.028em`
- **헤더 배경칠 금지. 헤더 아래 구분선 금지.**
- 색 띠와 배경칠은 정보를 나누는 가장 손쉬운 수단이라, 반복하면 관리자 페이지가 된다.

### D4. 깊이는 선이 아니라 그림자
- 카드 테두리 → 그림자
  `--e-card: 0 1px 2px rgba(16,24,40,.04), 0 12px 28px -16px rgba(16,24,40,.18)`
  `--e-lift: 0 1px 2px rgba(16,24,40,.05), 0 16px 32px -14px rgba(16,24,40,.22)` (hover)
- 카드 radius 9px → **14px**. 행·입력·버튼 radius 는 4/7/9 유지.
- `--bg` `#F7F8F9` → **`#F4F6F8`**. 카드 흰색과의 대비 확보.

### D5. 담당자는 이름으로
- **아바타 이니셜(권 · 조 · 박) 금지. 이름 그대로.** `--f6` / 600 / `--muted`
- 둘이면 `권정혁 · 조서연`, 셋 이상이면 `권정혁 외 2`.
- 이니셜은 동성이 둘이면 즉시 무너진다. 우리 팀 규모에서 아바타는 장식이다.
- **B-12 동봉:** 「팀 › 업무」 링크의 assignee 빈 값을 이 작업에서 함께 고친다.
  빈 값일 때 무엇을 그리는지(`담당 없음`)도 규격에 넣는다.

### D6. 간트 막대
- 높이 16px / radius 20px(완전 알약) / 안쪽 `inset 0 0 0 1px rgba(16,24,40,.05)`
- 완료 구간은 영역 색 실선, **남은 구간은 같은 색 opacity .16.** 회색으로 깔지 말 것.
- 기한 초과 막대는 `inset 0 0 0 1px rgba(199,56,60,.55)`
- 오늘 선 1px 코랄 opacity .45. **점은 눈금자에만 하나.**
  행마다 점을 찍으면 점의 세로 열이 된다. (시안 v6 에서 실제로 그랬다)
- 아이콘은 인라인 SVG 16px / `stroke-width:1.6` / `stroke-linecap:round` 통일.
  **문자 기호(▲ ● ▣ ▤ ↩ ◀)를 아이콘 자리에 쓰지 않는다.** 전수 교체 후 개수 제출.

### D7. 열 정렬
- 열 폭은 **고정값**이다. 내용에 따라 흔들리면 스캔이 안 된다.
  기준: 제목 296 · 영역 78 · 상태 76 · (막대 가변) · 담당 52 · D-day 44 · 진척 38
- 수치는 우측 정렬 + `font-variant-numeric: tabular-nums`

---

# §E 버튼 · 색 정리

### E1. 버튼 규격을 다시 쓴다 — **기존 규격이 틀렸다**
감사 결과: 중립 채움은 코드에 **0개**인데 규격이 그것을 요구했고, 흰 채움은 **69개**로
가장 많은데 규격에 없었고, 높이 4px 배수는 **416개 중 317개가 위반**이다.
317 대 99 면 코드가 아니라 규칙이 틀린 것이다. 다수를 위반으로 몰지 말고 규격을 고친다.

**4종만 둔다.**

| 종류 | 쓰임 | 제한 |
|---|---|---|
| 코랄 채움 | 그 화면의 주액션 | **화면당 1개** |
| 흰 채움 + 1px 선 | 보조 액션 | 없음 |
| 투명 + 텍스트 | 3차 · 링크형 | 없음 |
| 아이콘 전용 | 도구 막대 | `aria-label` 필수 |

- 높이는 **28 / 32 / 38** 세 가지만. **4px 배수 규칙은 폐기한다.**
- **회색 채움 전면 금지.** 지금 회색인 것은 흰 채움 또는 텍스트로 간다.
- 규격 밖 179개를 **전부** 위 4종에 배정하고 배정표(파일 · 라인 · 이전 · 이후) 제출.
- `scripts/audit-031.mjs` 의 규격 상수를 위 값으로 갱신하고 다시 돌릴 것.

### E2. 색
- **새 색 · 새 토큰 금지.** `--glass` 포함 기존 토큰 안에서 해결한다.
- 대비 4.5:1 미만 조합을 전수 조사해 목록 제출. `--faint` 위에 올라간 것부터.

### E3. 중첩 button (B-15)
- 분기 헤더의 `button` 안 `button` 을 푼다. 바깥을 `div[role=button]` 으로 바꾸지 말 것 —
  그건 같은 문제를 접근성 트리에서만 숨기는 것이다.
- 바깥을 헤더 영역(비대화형)으로 두고 **안쪽에 접기 토글과 액션을 형제로** 배치한다.
- 같은 형태가 다른 화면에도 있는지 전수 조사해 함께 고치고 개수를 제출.

---

# §F 모션 확장 (§H 개정)

### F1. 진입 스윕 — 화면 진입 시 1회
- 대상: **지연 · 이번 주 마감(D-0~D-7) 건의 간트 막대만.** 나머지는 처음부터 그려져 있다.
- 컨테이너 `opacity` 페이드(`--dur-3`), 안쪽 채움 `transform: scaleX(.04 → 1)`,
  `transform-origin: left center`, `--dur-4`, `--ease-out`
- 행마다 `--stagger-2`(60ms) 지연
- **1회만.** 재생 후 다시 움직이지 않는다. 필터·정렬 변경으로 재생하지 않는다.

### F2. §H 동시 개수 제한 개정
현행 "동시 3개"에 스윕이 걸린다. 규칙이 두 가지를 구분하지 못하고 있다.
서로 무관한 것 셋이 각자 움직이는 것과, 한 덩어리가 순차로 그려지는 것은 다르다.

> **개정.** 진입 1회 스태거 시퀀스는 동시 개수 제한에서 제외한다.
> 대신 **첫 시작부터 마지막 종료까지 1초를 넘지 않는다.**
> (스태거 × 건수 + 지속 ≤ 1000ms. 건수가 많으면 스태거를 줄이지 말고 **대상을 줄인다.**)

- `motion-audit` 에 시퀀스 총 길이 판정을 추가한다. 1초 초과면 위반.

### F3. 상시 호흡 — 화면에 정확히 하나
- **오늘 선의 점 하나만** 2.4초 주기로 호흡한다.
  `opacity .55 ↔ 1`, `transform: scale(1 ↔ 1.22)`, `ease-in-out`
- 개별 업무 행은 상시로 움직이지 않는다. 지연이 7건이면 7개가 깜빡이고,
  그러면 어느 것도 급해 보이지 않는다.
- `motion-audit` 판정 추가: `animation-iteration-count: infinite` 는
  **스켈레톤 시머와 이 점 하나** 외에 존재할 수 없다.

### F4. 상태 전환 — **이건 모션이 아니라 버그다**
- 클라이언트 타이머를 둔다. **자정 경과 시 D-day 를 다시 계산한다.**
  지금은 새로고침하지 않으면 날짜가 넘어가지 않는다. 화면 켜두고 출근하면
  어제 날짜를 보고 있다.
- D-1 → 오늘, 오늘 → 지연으로 넘어가는 순간 **그 행만** 반응한다.
  칩 `background-color` 전이 `--dur-3`, 막대 링 `box-shadow` 전이 `--dur-3`. 그게 전부다.
- **활동 로그에 남기지 않는다.** 시간이 흐른 것은 사건이 아니다.

### F5. 전제 조건
- `prefers-reduced-motion: reduce` — 스윕·호흡 전부 정지, 최종 상태로 고정. `!important`.
  (027 에서 `.agf-fab` 이 특이도로 빠져나간 전례가 있다. **전수 확인할 것.**)
- `visibilitychange` — 탭이 백그라운드면 상시 호흡 정지.
- 애니메이트 가능 속성은 §H 그대로. `width/height/top/left/margin/padding` 금지.

---

# §G 하지 않는 것

- 허들룸 재설계 (032)
- WIP 제한 · GitHub 개발 연동 · 저장된 필터 확장 (Jira 2순위)
- 새 색 · 새 시각 토큰 · 새 모션 토큰
- 데이터 정리 (업무 #32 · #42 중복 처리, 미연결 39건 배정) — PM 이 수동으로 한다
- 다크 모드
- B-5 · B-8 · B-10 · B-13 · B-14

---

# §H 검증

### 순서
§0 을 먼저 끝낸다. §0 의 SQL 이 실행되기 전에는 §A 이후를 푸시하지 않는다.
두 가지가 한 배포에 섞이면 순서가 바뀐 원인이 코드인지 데이터인지 못 가린다.

### 항목
- **0-4**: `ORDER BY` 원문 · `NULL` 처리 · 하위 업무 포함 여부 각각 한 줄.
- **0-3**: 정규화 전후 상위 20행 비교표. 중복 0건 증명. 0 이었던 행의 이동 목록.
- **A1**: 100% 배율 캡처. 하드코딩 `font-size` 개수 before/after. **11px 미만 0건 증명.**
- **A3**: 549건 → 잔여 건수. 상단 정렬이 맞는 예외는 별도 목록.
- **B2**: `density-audit` 실행 결과. 위반 0건 또는 허용 목록. **검사기 코드 경로 명시.**
- **C1**: 타일 4개 각각의 쿼리. `내가 막는 것` 은 실제 데이터로 1건 이상 만들어 캡처.
- **C4**: 같은 컴포넌트가 4곳에서 쓰이는지 **import 경로로** 증명. 캡처만으로는 부족하다.
- **D2**: 14종 → 5종 매핑표.
- **D6**: 문자 기호 전수 교체 개수. **오늘 점이 화면에 몇 개인지 실측(1개여야 한다).**
- **E1**: 179개 배정표. 회색 채움 0건 증명 — **계산된 스타일로** 확인할 것.
- **E3**: 중첩 button 전수 목록과 수정 후 0건.
- **F1/F2**: 스윕 시퀀스 총 길이(ms) 실측. `motion-audit` 신규 판정 통과.
- **F3**: `infinite` 애니메이션 전수 목록. 스켈레톤 + 점 1개 외 없음을 증명.
- **F4**: 시스템 시각을 조작해 자정을 넘겨 D-day 가 갱신되는지 실측. before/after 캡처.
- **F5**: `reduce` · 백그라운드 탭 각각 실측.

### 규격 외 발견 시
- 같은 규격 위반을 추가로 발견하면 **진행하고 보고**한다.
- 새 기능 · 새 규칙 · 데이터 변경 · 파일 삭제는 **승인 먼저.**
- 마이그레이션이 필요해지면 **푸시 전에 멈추고 보고.** (이번 건은 없을 것으로 본다)
- 부재 단언에는 **확인 방법과 짝이 되는 존재 단언**을 붙일 것.
