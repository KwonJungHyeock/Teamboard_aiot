# 병합 보고 — 015 · 018 · 019 · 021 → main (MD-P-2026-021 회신 ①)

작성 2026-08-05

---

## 1. 병합 결과

| 항목 | 값 |
| --- | --- |
| 병합 전 main | `1fb14b1` docs: MD-P-2026-016 병합 기록 + 스모크 결과 |
| **병합 커밋** | **`ad62b2c`** Merge MD-P-2026-015 · 018 · 019 · 021 into main |
| 병합 방식 | `--no-ff` (브랜치 이력 보존) |
| 충돌 | 없음 |
| origin/main 반영 | `1fb14b1..ad62b2c` 푸시 완료 |

포함된 커밋

| 해시 | 문서 | 내용 |
| --- | --- | --- |
| `8dee97a` | MD-P-2026-015 | 팀 오픈 준비 — 첫 사용 안내, 권한 전수(**개인 목표 권한 우회 수정**), 404/500, e2e, RUNBOOK·ONBOARDING |
| `06759c2` | MD-P-2026-018 | 팀원 피드백 선조치 — 활동 패널, 읽음 표시, 시스템 액션바, 정렬, 완료 D-day, 목표 연결 판별 |
| `e615f90` | MD-P-2026-019 | 디자인 개편 Phase 1 — 토큰 전면 교체, PageShell·DataList 공통 뼈대, 문서형 업무 상세 |
| `927cf07` | MD-P-2026-021 | 문서 경로 정리 — orders·reports·design, 캡처 문서번호 폴더화, docs/README.md |
| `92d578f` | MD-P-2026-021 | prototype v0.3 → archive, orders 보관 규칙 기록 |

병합 전 검사 — `npx tsc --noEmit` **0 오류**, `npm run build` **성공**(전 라우트 빌드).

---

## 2. 스모크 결과

### ⚠️ 프로덕션 접근 불가 — 로컬 프로덕션 빌드로 대체 검증

`https://teamboard-aiot.vercel.app` 은 이 실행 환경의 아웃바운드 프록시 정책에서 차단된다.

```
CONNECT teamboard-aiot.vercel.app:443 HTTP/1.1
< HTTP/1.1 403 Forbidden
gateway answered 403 to CONNECT (policy denial or upstream failure)
```

전 경로(`/`, `/login`, `/tasks`, `/goals`, `/reports`) 응답 코드 `000`(연결 실패).
**따라서 아래 결과는 프로덕션이 아니라 병합된 main을 로컬에서 `npm run build && next start` 로 띄운 것이다.**
프로덕션 확인은 Vercel 배포 완료 후 사람이 직접 봐야 한다(§4).

### 검증 조건

| 항목 | 값 |
| --- | --- |
| 대상 | main `ad62b2c` |
| 서버 | `next start` (프로덕션 빌드), `localhost:3411` |
| DB | 로컬 Postgres `127.0.0.1:5432/teamboard` (팀 데이터 시드). **프로덕션 DB 아님** |
| 계정 | 임시 lead 계정 `smoke-md021@robodyne.test` — 검증 후 삭제 완료(actor·account·activity_log·notification·read_marker) |
| 브라우저 | Chromium 1194 · 1440×950 · ko-KR |

### 결과 — 7/7 통과, 콘솔 오류 0, 5xx 0

| # | 경로 | 결과 | 근거 |
| --- | --- | --- | --- |
| 1 | 로그인 `/login` → `/assistant` | **O** | 세션 생성, 리다이렉트 정상 |
| 2 | 홈 `/` | **O** | `"좋은 오후예요, 스모크님"` 렌더 |
| 3 | 내 업무 `/tasks` | **O** | 제목 "내 업무", 빈 상태 정상(이 계정에 배정 업무 없음) |
| 3b | 내 업무 — 필터 해제 | **O** | 데이터 행 16개 · **행 높이 38px · 폰트 12.5px** (MD-P-019 §C 규격 일치) |
| 4 | 업무 상세 `/tasks?panel=task:84` | **O** | 문서형 패널 `.tdp` 1개 · 문서 영역 3블록 |
| 5 | 활동 `/inbox` | **O** | 200, 오류 없음 |
| 6 | 목표 `/goals` | **O** | 목표 요소 3개 렌더 |
| 7 | 월간 보고 `/reports` | **O** | 제목 "월간 보고" |

**#3의 "1행"에 대한 확인** — 최초 측정에서 행 높이가 133px로 나왔다. 원인을 확인한 결과
그 행은 데이터 행이 아니라 `colspan=9` **빈 상태 행**("아직 업무가 없어요")이었다.
임시 계정에 배정된 업무가 없어서 나온 정상 동작이며, 필터를 해제하고 실제 데이터 16행을 측정하니
**38px / 12.5px** 로 규격에 맞았다. 회귀 아님.

캡처 `docs/shots/MD-P-2026-021/` 8장
(`21-smoke-1-login` `-2-home` `-3-tasks` `-3b-tasks-data` `-4-task-detail` `-5-activity` `-6-goals` `-7-reports`)

---

## 3. 마이그레이션 확인

병합으로 들어온 신규 마이그레이션이 로컬 DB에 적용됨을 스키마로 확인했다.

| 마이그레이션 | 확인 |
| --- | --- |
| `0021_onboarding.sql` | `account.onboarded_at TIMESTAMPTZ` 존재 |
| `0022_task_doc.sql` | `task.doc` / `doc_updated_at` / `doc_updated_by`(FK → actor) 존재 |

마이그레이션은 첫 DB 접근 시 자동 적용된다. **프로덕션에서도 첫 요청 때 0021·0022가 적용된다.**

---

## 4. 프로덕션에서 확인이 필요한 항목

이 환경에서 확인할 수 없어 배포 후 직접 봐야 하는 것들.

| # | 항목 | 확인 방법 |
| --- | --- | --- |
| 1 | Vercel 배포 성공 여부 | Vercel 대시보드에서 `ad62b2c` 배포 상태 |
| 2 | 0021·0022 마이그레이션 자동 적용 | 첫 로그인 후 업무 상세를 열어 문서 편집기가 뜨는지 |
| 3 | 첫 사용 안내 3장 | 팀원 최초 로그인 시 노출(`onboarded_at` NULL 기준) |
| 4 | 개인 목표 권한 수정(015) | member 계정으로 타인 개인 목표 API 직접 호출 → 403 |
| 5 | 실 데이터 기준 화면 밀도 | 로컬은 시드 데이터 기준 |

---

## 5. 다음 작업

**MD-P-2026-020** — 홈 재구성 §D · 속성 블록 배선 §F1 · 연결 리소스 자동 집계 §F3.

- `components/PropertyBlock.tsx` 는 MD-P-019에서 만들어졌으나 `TaskDetailPanel` 에 아직 배선되지 않았다.
- **시안이 도착했다.** 병합 직후 `6a7e105`(GitHub 업로드)로 `docs/design/missiondeck_redesign.html` 이 올라왔고,
  문서번호 규칙에 맞춰 `docs/design/MD-P-2026-019_핵심3화면.html` 로 개명했다(`git mv`).
  `<title>Mission Deck — 개편 시안 (핵심 3화면)</title>`, 30,188 B, 홈·내 업무·업무 상세 3화면.
  배치 의도 참고용으로 사용하되 **수치는 지시서를 우선한다.**
- 시안의 `--muted:#8A929C` 는 흰 배경 대비 3.15:1 로 §G의 4.5:1 기준에 미달한다.
  구현은 `#666C73`(5.31:1)을 쓴다 — MD-P-019 완료보고에 기재된 기존 편차이며 유지한다.
