# 완료 보고 — 캔버스 이미지 활성화 + Private Blob 대응 (MD-P-2026-014 · 014a)

작성 2026-08-05 · 브랜치 `claude/new-session-58fqtb`

---

## 0. 전제 확인

| 항목 | 결과 |
| --- | --- |
| `@vercel/blob` ≥ 2.3 | **충족** — `^2.6.1` 선언, `node_modules` 실제 설치본 `2.6.1` |
| `access: 'private'` · `get(ifNoneMatch)` 지원 | 확인 — `GetCommandOptions.ifNoneMatch`, `GetBlobResult` 가 `statusCode 200 | 304` 판별 유니온 |
| 환경변수 (Vercel) | 팀장 확인 — `BLOB_READ_WRITE_TOKEN` · `BLOB_STORE_ID` 를 Production/Preview/Development 에 등록 |
| 환경변수 (이 개발 컨테이너) | **없음** — MD-P-2026-016 ① 결정("개발 환경에 운영 비밀값을 내려보내지 않는다")을 그대로 유지 |

작업을 중단하지 않은 이유: 중단 조건은 **실행 환경(Vercel)에 자격 증명이 없을 때**다.
배포 대상에는 등록돼 있고, 로컬에 없는 것은 016 ①에서 이미 결정된 구조다.
그래서 실제 SDK 경로로 구현하고 **검증만 로컬/프로덕션으로 나눠** 보고한다(§4).

---

## 1. 작업 요약 (3줄)

1. 업로드를 `put(pathname, file, { access: 'private' })` 로 바꾸고, DB에는 공개 URL이 아니라 **pathname·원본 파일명·크기·contentType** 을 저장한다.
2. 인증·인가를 **라우트 핸들러 안에서 직접** 하는 스트리밍 경로 `GET /api/blob` 을 만들고, 화면의 모든 이미지가 이 경로만 쓰게 했다.
3. 공개 URL을 만들던 구 업로드 라우트를 폐쇄(410)했고, 블록이 사라지면 blob 객체도 지운다.

`tsc --noEmit` 0오류 · `npm run build` 성공 · 전 16화면 200 / 콘솔·5xx 오류 0.

---

## 2. [작업 범위]

### §A 업로드 (014 §A + 014a §A)

| 항목 | 결과 |
| --- | --- |
| `access: 'private'` | O — `lib/blob.ts` `putPrivate()` 가 `put(pathname, body, { access: "private", contentType })` 호출 |
| 공개 URL 미생성 | O — 코드 전체에 `access: "public"` **0건** (`grep` 확인) |
| DB 저장값 | O — `pathname` · `name` · `size` · `contentType`. `url` 필드를 쓰지 않는다 |
| 추측 불가 세그먼트 | O — `projects/{id}/canvas/{uuid}-{원본파일명}` (`randomUUID()`) |
| 업로드 상한 10MB | O — 초과 시 **413** |
| 허용 타입 png·jpg·webp·gif | O — 그 외 **415** (svg 차단 확인) |
| 서버 라우트 경유 · 토큰 미노출 | O — `POST /api/blob/upload`. 클라이언트는 토큰을 보지 못한다 |

경로 규칙 4종 — `projects/{id}/canvas` · `tasks/{id}/doc` · `review/{sessionId}` · `signals/{id}`

### §B 이미지 전달 경로 (014a §B, 신규)

`GET /api/blob?pathname=…`

| 항목 | 결과 |
| --- | --- |
| ① 핸들러 안에서 직접 인증 | O — `app/api/blob/route.ts` 첫 줄이 `getSession()` |
| **미들웨어에 위임하지 않음** | O — `middleware.ts` 는 `/api` 를 그대로 통과시킨다. 이 라우트의 인증은 전적으로 핸들러 책임 |
| ② `get(pathname, { access: 'private' })` | O |
| ③ 스트림 응답 | O — 버퍼링 없이 `ReadableStream` 을 그대로 전달 |
| 권한 없음 → **404** | O — 403이 아니라 404. 존재 여부도 노출하지 않는다 |
| `Content-Type` | O — blob 의 `contentType` |
| `X-Content-Type-Options: nosniff` | O |
| `Cache-Control: private, no-cache` | O |
| `ETag` | O — `blob.etag` 그대로 |
| 조건부 요청 | O — 요청의 `If-None-Match` → `get(ifNoneMatch)`, `statusCode === 304` 면 **본문 없이 304** |
| `s-maxage` 등 CDN 지시자 | **없음** — 단위 검증에서 `s-maxage|public|immutable` 미포함 확인 |

**권한 판정** (`lib/blob-access.ts`) — 두 조건을 모두 만족해야 통과한다.

1. **그 엔티티에 실제로 붙어 있는 pathname 인가** — 캔버스 blocks / 업무 doc / 코멘트·시그널 `image_url` / 리뷰 항목을 JSONB·컬럼으로 직접 조회
2. **그 엔티티를 볼 권한이 있는가** — 시그널은 기존 `signalVisibilityClause()` 재사용(판정 로직 중복 금지)

부수 효과로 **본문에서 지운 이미지는 그 즉시 라우트로도 열리지 않는다**(참조가 사라지므로).

### §C 화면 표시 (014a §C)

| 항목 | 결과 |
| --- | --- |
| 이미지 `src` = 라우트 경로 | O — `blobSrc(pathname)` = `/api/blob?pathname=…` |
| blob 원본 URL 노출 | **없음** — `*.blob.vercel-storage.com` 하드코드 0건(주석 1건 제외) |
| 썸네일·라이트박스 동일 경로 | O — `components/BlobImage.tsx` 한 벌. 라이트박스는 `--scrim-strong`(MD-P-2026-016 ② 예외) |
| 실패 시 사유 + [다시 시도] | O — 깨진 이미지 대신 "권한이 없거나 파일이 삭제됐을 수 있습니다" + 재시도 버튼 |

적용 화면: 프로젝트 캔버스 · 업무 문서 본문 · 업무 설명/코멘트 마크다운 · 시그널/코멘트 첨부(`ImageThumb`).
예전에 저장된 `http…` URL 도 같은 컴포넌트가 그대로 표시한다(데이터 마이그레이션 불필요).

### §D 삭제 (014 §C + 014a §D)

| 항목 | 결과 |
| --- | --- |
| 캔버스 블록 삭제 → blob 삭제 | O — 저장 시 이전 blocks 와 비교해 사라진 pathname 을 `del()` |
| 업무 문서 블록 삭제 → blob 삭제 | O — 동일 규칙 |
| 실패 시 저장 자체는 진행 | O — 삭제 실패가 문서 저장을 막지 않는다 |
| 응답에 `removedBlobs` 개수 | O — 검증·감사용 |

### §D(014) 동시 편집 · §F 스크림

- `baseUpdatedAt` → 409 규칙은 캔버스·업무 문서 모두 **기존 그대로** 유지(이번에 건드리지 않음).
- 스크림 통일(§F)은 MD-P-2026-014 1차에서 이미 완료됨. 라이트박스만 `--scrim-strong` 예외.

### §E 리뷰·코멘트 첨부

| 항목 | 결과 |
| --- | --- |
| 코멘트 이미지 첨부 UI | O — 논의·결정 스레드 컴포저에 `🖼 이미지 첨부` + 미리보기 + 제거 |
| 코멘트 API가 pathname 수용 | O — `signals/{id}` scope 가 일치할 때만 저장(남의 경로 주입 차단) |
| 시그널/코멘트 이미지 표시 | O — `ImageThumb` → `BlobImage` |

---

## 3. [기준과 다르게]

### ① 구 공개 업로드 라우트를 삭제하지 않고 **410** 으로 폐쇄했다

`/api/review/upload` 는 `access: 'public'` 으로 blob 을 만들던 경로다. Private 스토어에서
이 경로를 남겨두면 URL 유출 = 열람이 된다. 라우트 파일을 지우면 호출자가 404 만 보고 원인을 못 찾으므로,
**410 Gone + 대체 경로 안내**로 바꿨다. 기능이 아니라 위험 경로를 닫은 것이다.

### ② 시그널 **생성 시점**의 파일 첨부는 넣지 않았다 — 회신 ③에서 **현행 유지 확정**

경로 규칙이 `signals/{id}/…` 인데 생성 시점에는 id 가 없어 경로를 검증할 수 없다.
검증 없이 받으면 남의 시그널 경로를 심을 수 있다. 그래서 생성 폼은 기존대로 URL 입력만 두고,
**파일 첨부는 생성 후 코멘트로** 올리게 했다. (허들 메모 폼의 "이미지 URL" 입력은 그대로 유지)

### ③ 권한 없음에 403이 아니라 **404**

지시서 §B 그대로다. 다만 이 때문에 "권한 없음"과 "스토리지 오류"가 응답으로 구분되지 않는다.
의도한 트레이드오프이며, 사용자에게는 §C 의 실패 안내로 설명한다.

---

### 회신 반영 (MD-P-2026-014·014a 결정)

| # | 결정 | 상태 |
| --- | --- | --- |
| ① | Preview 에서 §E 5건 검증 후 main 병합 | `712eb58` 브랜치 유지, main 미변경 |
| ② | §E 검증은 Preview 배포에서 수행(프로덕션 불필요) | §4-5 절차 참조 |
| ③ | 시그널 생성 시점 첨부 — 현행 유지, 2단계 저장 도입 안 함 | 변경 없음 |
| ④ | [기준과 다르게] 3건 전부 승인 | 그대로 유지 |

---

## 4. 검증

### 4-1. 단위 — 22/22 통과

`lib/blob.ts` 의 순수 함수를 직접 호출해 검증했다(모의 객체 없음).

| 항목 | 결과 |
| --- | --- |
| `parseScope` 4종 정상 해석 | O |
| UUID 세그먼트 없는 경로 거부 | O |
| **경로 이탈(`../`) 거부** | O |
| 알 수 없는 접두사·빈 문자열 거부 | O |
| `safeName` 경로 제거(`../../etc/passwd` → `passwd`) | O |
| 한글 파일명 보존 | O |
| 200 응답 헤더 4종(Content-Type·nosniff·private,no-cache·ETag) | O |
| **CDN 캐시 지시자 미포함**(`s-maxage`/`public`/`immutable`) | O |
| 304 — 상태코드·본문 없음·ETag 유지 | O |
| 게이트: 토큰 없음 → `none`, RW만 → `token`, **OIDC 우선** | O |

### 4-2. 런타임 인가 — 19/19 통과

프로덕션 빌드(`next start`)에 **게이트만 열고**(`BLOB_READ_WRITE_TOKEN=dummy`, `.env.local` 에는 쓰지 않음)
인가 계층을 실제로 태웠다. 인가 판정은 SDK 호출 **이전**에 끝나므로 결과가 유효하다.

**§E-1 로그아웃 상태에서 이미지 라우트 직접 호출**

| 검사 | 결과 |
| --- | --- |
| 세션 없음 → **401** | O |
| 본문 노출 | **0바이트** |
| 로그인 HTML 리다이렉트 아님 | O (302/307 아님, HTML 없음) |

**§E-2 권한/경로 판정 → 전부 404**

| 검사 | 결과 |
| --- | --- |
| 규칙 밖 경로(UUID 없음) | 404 |
| **경로 이탈 시도** `…/{uuid}-../../etc/passwd` | 404 |
| 알 수 없는 접두사 `secrets/…` | 404 |
| 없는 프로젝트 | 404 |
| **참조되지 않은 파일(경로를 정확히 맞춰도)** | 404 |
| 참조되지 않은 업무 파일 | 404 |
| `pathname` 파라미터 없음 | 404 |

**§E-2 권한 없는 계정** — 업로드 라우트는 인가 결과가 상태코드로 갈려 판정이 명확하다.

| 검사 | 결과 |
| --- | --- |
| 조서연(member)이 **타인의 비공개 메모**(권정혁 소유)에 업로드 | **404** |
| 권정혁(소유자)이 같은 대상에 업로드 | 인가 통과 → SDK 단계 도달 |
| 없는 업무 | 404 |
| 세션 없이 업로드 | 401 |

**§A 업로드 제약**

| 검사 | 결과 |
| --- | --- |
| svg | **415** |
| 11MB | **413** |
| 알 수 없는 kind | 400 |

**공개 경로 폐쇄** — 구 `/api/review/upload` → **410**

**스토리지 미연결(게이트 닫힘) 상태**

| 검사 | 결과 |
| --- | --- |
| 업로드 | **503** + 안내 문구 |
| 읽기 | **404** |
| `doc`/`canvas` API 의 `blobReady` | `false` → UI가 이미지 버튼을 비활성 |

### 4-3. UI

| 검사 | 결과 |
| --- | --- |
| 캔버스 `＋ 이미지` 버튼 | **활성** (`disabled=false`, title `png · jpg · webp · gif · 10MB 이하`) |
| 캔버스 버튼 구성 | `＋ 텍스트 | ＋ 체크리스트 | ＋ 링크 | ＋ 이미지` |
| 업무 문서 본문 영역 | 렌더 정상 |

### 4-4. 회귀 — 전 16화면 200, 오류 0

`/` `/tasks` `/calendar` `/goals` `/projects` `/projects/1` `/signals` `/huddle` `/reports`
`/inbox` `/members` `/assistant` `/settings` `/handover` `/saved` `/status`

### 4-5. Preview 배포에서 확인할 항목 (회신 ② — 팀장 수행)

이 개발 환경에는 실제 blob 자격 증명이 없고, 아웃바운드 프록시가 `blob.vercel-storage.com` 을 차단한다.
아래 5건은 **실제 blob 객체가 있어야만** 판정되므로 Preview 배포에서 확인한다.
대상: 브랜치 `claude/new-session-58fqtb` 의 Preview URL.

| # | 항목 | 절차 | 통과 기준 |
| --- | --- | --- | --- |
| 1 | 업로드·표시 | 프로젝트 → `캔버스` 탭 → `＋ 이미지` → png 선택 | 카드에 이미지가 뜬다. `img src` 가 `/api/blob?pathname=…` |
| 2 | **원본 URL 직접 접근** | Vercel → Storage → Blob 에서 방금 객체의 URL 복사 → 새 탭 주소창에 붙여넣기 | **열리지 않는다** (403/404) |
| 3 | **재요청 304** | 개발자도구 Network 열고 새로고침 → `/api/blob?pathname=…` 행 확인 | `304` · 요청 `If-None-Match` 와 응답 `ETag` 일치 · `Cache-Control: private, no-cache` |
| 4 | **삭제 시 객체 소멸** | 이미지 블록 `✕` → 800ms 뒤 저장 → Blob 대시보드 새로고침 | 객체가 사라진다. Network 의 canvas PUT 응답에 `removedBlobs: 1` |
| 5 | OIDC 경로 | Network 에서 `POST /api/blob/upload` 응답 JSON 확인 | `auth: "oidc"` (OIDC 미사용 프로젝트면 `"token"` — 둘 다 정상) |

**실패 시 볼 곳**

- 4번에서 객체가 남아 있으면 canvas/doc PUT 응답의 **`blobDeleteError`** 필드에 원인이 담긴다(이번에 추가).
- 1번에서 이미지가 안 뜨면 화면에 "권한이 없거나 파일이 삭제됐을 수 있습니다 + [다시 시도]" 가 나온다.
  이때 Network 의 `/api/blob` 응답 코드를 함께 전달하면 401(세션)/404(권한·경로)/기타를 구분할 수 있다.
- 업로드가 503 이면 그 환경에 자격 증명이 없다는 뜻이다(`BLOB_STORE_ID` 포함).

> 위 5건의 **앞단**(인증·인가·경로 규칙·응답 헤더·삭제 대상 산출)은 §4-1·4-2 에서 전부 검증했다.
> Preview 에서 확인할 것은 "실제 객체가 오가는 구간" 하나뿐이다.

---

## 5. 변경 파일

| 파일 | 변경 |
| --- | --- |
| `lib/blob.ts` | **신규** — put/get/del(private) · 경로 규칙 · `buildBlobResponse` · OIDC 우선 게이트 |
| `lib/blob-access.ts` | **신규** — 읽기/쓰기 권한 판정(참조 확인 + 기존 가시성 규칙 재사용) |
| `app/api/blob/route.ts` | **신규** — 인증→인가→스트림. 304 지원 |
| `app/api/blob/upload/route.ts` | **신규** — private 업로드. pathname 만 반환 |
| `app/api/review/upload/route.ts` | 공개 URL 생성 경로 폐쇄(410) |
| `lib/upload.ts` | pathname 기반으로 교체 + `blobSrc`/`imageSrc` |
| `components/BlobImage.tsx` | **신규** — 표시·실패 안내·재시도·라이트박스 |
| `components/ProjectCanvas.tsx` | 이미지 블록 활성화(버튼·붙여넣기·드롭) |
| `components/DocEditor.tsx` | 이미지 블록 업로드·표시 |
| `components/TaskDetailPanel.tsx` | 설명/코멘트 이미지 삽입을 라우트 경로로 |
| `components/SignalThread.tsx` | 코멘트 이미지 첨부 UI |
| `components/huddle-ui.tsx` | `ImageThumb` → `BlobImage` |
| `app/api/projects/[id]/canvas/route.ts` | 이미지 블록 저장 허용 + 고아 blob 삭제 |
| `app/api/tasks/[id]/doc/route.ts` | 동일 + `blobEnabled()` 기준으로 게이트 교체 |
| `app/api/signals/[id]/comments/route.ts` | 코멘트 첨부 pathname 수용(scope 일치 검사) |
| `lib/projects.ts` | `CanvasBlock` 에 이미지 필드 |
| `app/design.css` | `.bimg*` · `.cmt-attach*` · `.pcv-img*` |

**신규 시각 토큰 0개.** 라이트박스는 기존 `--scrim-strong` 을 쓴다.

---

## 6. 비용 메모

Private 은 Function 경유라 공개 저장보다 전송 비용이 높다(014a).
업로드 상한 10MB 를 유지해 "100MB 초과 파일을 이 경로로 서빙하지 않는다"는 조건을 자동으로 만족한다.
같은 이미지 재요청은 `ETag` + `304` 로 본문 전송을 피한다.
