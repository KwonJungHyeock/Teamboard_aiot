# 결정 회신 반영 — MD-P-2026-016

작성 2026-08-04 · 브랜치 `claude/new-session-58fqtb` → `main`

## 작업 요약 (3줄)

1. **③ 히어로 바** — 명도 하향(65%)을 철회하고 배경을 영역색 원본으로 복원했다. 대신 바마다 흰색·차콜 중 대비가 높은 쪽을 골라 글자색으로 쓴다. **영역색 6종 전부 4.5:1 통과**(보라만 흰색, 나머지 5종 차콜).
2. **② 스크림** — `--scrim-strong: rgba(35,43,54,.78)` 토큰 1개를 추가해 라이트박스(`.rv-zoom`)만 분리했다. 나머지 7곳은 `--scrim` 유지.
3. **④ 병합** — `main`에 병합하고 스모크 6화면을 확인했다. 014 A~E(이미지 업로드)는 묶지 않았다.

---

## ③ 히어로 타임라인 바 — 대비 계측표

배경(영역색)은 그대로 두고 글자색만 바꾼다. 코드가 실제로 부여하는 조합을 브라우저에서 계측했다.

| 영역 | 바 배경 | 흰 글씨 | 차콜 글씨 `--ink #1C232E` | **코드 선택** | 최종 대비 | 판정 |
|------|---------|--------:|--------:|:---:|--------:|:----:|
| EDUINO(파랑) | `--blue` `#4B8DF8` | 3.25 | **4.86** | 차콜 | 4.86:1 | 통과 |
| Playino(보라) | `--purple` `#7C5CE0` | **4.70** | 3.36 | **흰색** | 4.70:1 | 통과 |
| 추론(틸) | `--teal` `#0FA79A` | 2.99 | **5.28** | 차콜 | 5.28:1 | 통과 |
| 초록 | `--green` `#4C9A6E` | 3.41 | **4.63** | 차콜 | 4.63:1 | 통과 |
| 앰버 | `--amber` `#D8A22E` | 2.30 | **6.87** | 차콜 | 6.87:1 | 통과 |
| 팀(슬레이트) | `--slate` `#98A1B0` | 2.61 | **6.06** | 차콜 | 6.06:1 | 통과 |

**6종 중 미달 0종.** 보라만 배경이 충분히 어두워 흰 글씨가 이긴다 — 지시서가 말한 "원래부터 어두운 바는 흰 텍스트 유지"에 해당하는 유일한 케이스다.

### 함께 잡은 것 — 부제목 불투명도

`.gt2-bar-s`(담당·진척% 라벨)에 `opacity: .85`가 걸려 있어 글자가 바 색과 섞이면서 대비가 0.9~1.0 깎였다. 차콜을 써도 파랑에서 4.86 → **3.92**로 떨어져 미달이 된다. 불투명도를 제거하고 위계는 `font-weight: 500`(제목은 700)으로 준다.

### 실제 렌더 계측 (요약·상세 두 모드 전수)

| 모드 | 요소 | 배경 | 글자 | 크기/굵기 | 대비 | 판정 |
|------|------|------|------|-----------|-----:|:----:|
| 요약 | `.gt2-bar-t` | `#4B8DF8` | `#1C232E` | 11.5 / 700 | 4.86 | 통과 |
| 요약 | `.gt2-warn` ⚠ | `#4B8DF8` | `#1C232E` | 11 / 400 | 4.86 | 통과 |
| 요약 | `.gt2-bar-t` | `#0FA79A` | `#1C232E` | 11.5 / 700 | 5.28 | 통과 |
| 상세 | `.gt2-bar-t` | `#4B8DF8` | `#1C232E` | 11.5 / 700 | 4.86 | 통과 |
| 상세 | `.gt2-bar-s` | `#4B8DF8` | `#1C232E` | 10.5 / 500 | 4.86 | 통과 |
| 상세 | `.gt2-bar-t` | `#0FA79A` | `#1C232E` | 11.5 / 700 | 5.28 | 통과 |
| 상세 | `.gt2-bar-s` | `#0FA79A` | `#1C232E` | 10.5 / 500 | 5.28 | 통과 |

시드 데이터에 파랑·틸 두 영역만 있어 나머지 4색은 위의 6종 표(코드가 부여하는 조합을 주입해 계측)로 확인했다.

**지연 바**: 이 코드에서 `late`는 배경색을 바꾸지 않고 우측 코랄 캡(`.gt2-cap`)과 링만 더한다. 따라서 지연 여부와 무관하게 영역색 규칙을 그대로 따른다.

변경 파일 `components/HeroTimeline.tsx`(`areaOnColor()` 추가), `app/home.css`(`.gt2-bar` 기본 `color:#fff` 제거, `.gt2-bar-s` 불투명도 제거)

---

## ② 스크림 — 토큰 2개 체계

| 토큰 | 값 | 쓰는 곳 |
|------|-----|---------|
| `--scrim` | `rgba(35,43,54,.45)` | `.modal-backdrop` `.ovl` `.tdp-backdrop` `.dcf-bg` `.gpp-bg` `.gdlg-bg` `.ngm-backdrop` (7곳) |
| `--scrim-strong` | `rgba(35,43,54,.78)` | `.rv-zoom` (라이트박스 전용, 1곳) |

신규 토큰은 승인된 `--scrim-strong` 1개뿐. 하드코드 스크림은 0곳.

---

## ④ 병합 · 스모크

병합 커밋 해시와 스모크 결과는 아래 §병합 기록 참조.

---

## ⑤ 대비 계산 불가 964곳 — 백로그

조치 없음. 배경이 그라디언트이거나 반투명이 겹쳐 조상 탐색만으로 실제 배경색을 확정할 수 없는 요소들이다. 정확 판정에는 렌더 픽셀 샘플링 계측 도구가 필요하며, 이는 계측 도구의 한계이지 제품 결함이 아니다. 육안 확인 결과 대부분 사이드바·히어로(다크 앵커)의 흰 글씨다. **백로그 기록만 하고 이번 범위에서 제외.**

---

## 토큰

신규 1개 — `--scrim-strong: rgba(35,43,54,.78)`
직전 지시(MD-P-2026-014 §F)의 `--scrim: rgba(35,43,54,.45)` 포함 시 스크림 토큰은 총 2개.
그 외 신규 시각 토큰 없음.

## 캡처

| 파일 | 내용 |
|------|------|
| `docs/shots/16-hero-bar-before.png` | 요약 모드 — 흰 글씨(수정 전, 3.25:1) |
| `docs/shots/16-hero-bar-after.png` | 요약 모드 — 차콜 글씨(수정 후, 4.86:1) |
| `docs/shots/16-hero-bar-detail-before.png` | 상세 모드 — 흰 글씨 + 부제목 불투명도 .85 |
| `docs/shots/16-hero-bar-detail-after.png` | 상세 모드 — 차콜 글씨 + 불투명도 제거 |
| `docs/shots/16-scrim-compare.png` | `--scrim` .45 ⇄ `--scrim-strong` .78 나란히 비교 |
| `docs/shots/14-scrim-palette.png` · `14-scrim-modal.png` | 통일된 모달·팔레트 스크림 |

전후 캡처의 "before"는 수정 전 선언을 그대로 다시 주입해 같은 데이터·같은 화면에서 A/B로 찍었다. 3배 확대.

## 회귀 확인

- 대비 4.5:1 미달 **0곳** (19개 화면, 검사 613곳)
- 26개 화면 전수: HTTP 200 · 콘솔 에러 0 · 다크 표면 0 · mono 미적용 0 · 빈 상태 CTA 누락 0
