# 삭제 전 결정 로그 — MD-P-2026-024

**덤프 시각** 2026-08-06 (KST) · **덤프 대상 DB** 개발 로컬 (프로덕션과 동일 시드 기준)

## 왜 남기는가

MD-P-2026-024 [지시 4] 로 데모 시그널 8건(`is_demo = true`, id 1~8)을 하드 삭제하기로 확정했다.
`decision.discussion_id` 는 `NOT NULL` 이라 시그널을 지우면 그 시그널에서 파생된 **결정도 함께 지워진다.**

결정은 이 제품의 1급 기록이고 `decision` 테이블에는 `is_demo` 플래그가 없다.
그래서 **지우기 전에 전문을 여기에 남긴다.** 아래 3건이 삭제 대상의 전부이며,
이 시점 DB에 존재하는 결정도 이 3건이 전부다.

> 회신 3 [확정]: "시그널 8건이 삭제 승인된 이상 그로부터 파생된 결정만 남길 수 없습니다.
> 단, 삭제 전에 전문을 덤프해 커밋할 것."

---

## 결정 #1 — Playino는 빌드 통합 방식으로 간다

| 항목 | 값 |
| --- | --- |
| id | 1 |
| 상태 | `superseded` (결정 #3 으로 번복됨) |
| 결정 시각 | 2026-08-04 04:22 |
| 결정자 | 권정혁 |
| 참조 시그널 | #1 「Playino 빌드 통합 vs 동적 로딩 방식 확정」 |
| 프로젝트 | #2 (Playino) |
| 연결 업무 | `{84}` |

**본문**

```
- 권정혁: 동적 로딩은 초기 로딩이 느려 교육 현장에서 대기 시간이 길어짐
- 조서연: 빌드 통합이 배포 파이프라인과도 맞음
```

---

## 결정 #2 — 첫 에피소드는 카메라 미로찾기로 간다

| 항목 | 값 |
| --- | --- |
| id | 2 |
| 상태 | `confirmed` |
| 결정 시각 | 2026-08-04 05:08 |
| 결정자 | 박주희 |
| 참조 시그널 | #5 「카메라 게임 4종 중 첫 에피소드 선정 기준」 |
| 프로젝트 | — |
| 연결 업무 | 없음 |

**본문**

```
- 조서연: 카메라 인식 난이도가 가장 낮아 첫 차시에 적합
- 박주희: 교구 준비물이 적어 현장 세팅이 빠름
```

---

## 결정 #3 — 동적 로딩으로 되돌린다 — 빌드 시간이 한계

| 항목 | 값 |
| --- | --- |
| id | 3 |
| 상태 | `confirmed` (결정 #1 을 번복) |
| 결정 시각 | 2026-08-04 07:33 |
| 결정자 | 권정혁 |
| 참조 시그널 | #1 「Playino 빌드 통합 vs 동적 로딩 방식 확정」 |
| 프로젝트 | — |
| 연결 업무 | 없음 |

**본문**

```
- 권정혁: 통합 빌드가 12분을 넘겨 반복 실험이 막힘
```

---

## 함께 삭제되는 것

| 대상 | 건수 | 비고 |
| --- | --- | --- |
| 시그널 | 8건 (id 1~8) | `is_demo = true` |
| 결정 | **3건 (id 1~3)** | 위 전문 |
| 코멘트 | 31건 | 시그널 #1(7) · #2(15) · #5(7) · #6(2) — 시그널 종속이라 별도 승인 없이 삭제 |
| 업무 | 20건 (id 1~20) | `is_demo = true`, #10 포함 |
| 목표 | 7건 (id 1~7) | `is_demo = true` |

## 되돌리려면

프로덕션 삭제 전 스냅샷: Neon 브랜치 `backup-pre-024-20260806`.
이 문서의 값은 사람이 읽기 위한 기록이고, 실제 복원은 위 스냅샷에서 한다.
