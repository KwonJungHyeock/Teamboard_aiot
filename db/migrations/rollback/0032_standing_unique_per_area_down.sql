-- MD-P-2026-032 — 0032 되돌리기
--
-- 인덱스 하나뿐이다. 데이터를 만들지도 옮기지도 않았으므로
-- **되돌려도 잃는 것이 없다.** 시한을 두지 않는다(0029~0031 과 다른 점이다).
--
-- 되돌린 뒤에는 「영역당 상시 하나」를 아무도 지키지 않는다는 것만 기억한다.
-- §B 의 상시 버튼은 그 경우 id 최소값을 고르고 console.error 를 남긴다 —
-- 조용히 틀리지는 않지만, 옳아지지도 않는다.
--
-- `schema_migrations` 에서 줄을 **지우지 않는다**(RUNBOOK 규약).
-- 다시 적용해야 하면 **새 번호로 정방향 마이그레이션을 쓴다.**

DROP INDEX IF EXISTS project_standing_one_per_area;
